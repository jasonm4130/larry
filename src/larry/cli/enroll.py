def main(name: str) -> None:
    """Phase 4: Record 10s of <name>'s voice, compute Resemblyzer embedding, store in data/speakers.db."""
    print(f"enroll({name}) — not yet implemented; see plan Phase 4")
